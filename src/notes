+1) içecek class'ı yap
+2) Flavor enumeration
+3) yeni order geldiğinde içecek objesi oluştur, txt'ye obje olarak yazdır
+4) bst oluştur, objeleri içine at
5) menu management panel (see all eksik)
6) customer panel
7)



**** BINARY SEARCH TREE ****
new cocktail sayfasında submit ile oluşturulacak yeni kokteyller
txt dosyasına obje olarak yazdırılacak, programın açılışında bu txt dosyası
okunarak BST oluşturulacak. MenuManagement panelinde see all butonunu kullandığımızda BST içerisinden inorder
yöntemiyle okunup default list model içerisine eklenecektir.


**** QUEUE ****
yeni bir order geldiği zaman sıraya eklenecek ve staff menüsünde orders
sayfasında queue'deki elemanlar çekilerek gösterilecek, sırayla baştan aşağı
yazdırılacak


*** HATALAR ***


++++ ÇÖZÜLDÜ 1) Cocktails2 dosyasında hiç obje yoksa ilk objeyi override edilen method ile yapmamak gerekiyor çünkü bu method ile
=======
1) Cocktails2 dosyasında hiç obje yoksa ilk objeyi override edilen method ile yapmamak gerekiyor çünkü bu method ile

objelerin başına gelen header özelliğini kaldırdık. ancak ilk objenin başında bir header bulunması gerektiği için sıfırdan
BST veritabanı oluştururken normal ObjectOutputStream kullanılması gerekiyor.
Çözüm-> dosyanın boş olup olmadığını anlayabileceğimiz bir method varsa if else ile farklı output stream fonksiyonu kullanılabilir

2) Kokteyl isminde sayı kullanıldığında string olarak algıladığı için, başlangıcı aynı olup sonunda (12,15,121)
sayıları bulunduran içecekler için inorder sıralaması 12 - 121 - 15 gibi oluyor.
Çözüm-> isimde sayı kullanımı kısıtlanabilir

3) Program açıkken yeni kokteyl eklendiğinde tüm tariflerin olduğu AllRecipes sayfasında dinamik olarak eklenmiyor.

Program açılıp kapandıktan sonra değişiklik uygulanıyor.

4) File ismini dinamik olarak aktarmak için newcocktailpage'i allrecipes içerisine atmak gerekiyor yoksa
file ismini okurken manuel girmek durumundayız
=======
Program açılıp kapandıktan sonra değişiklik uygulanıyor.

